import React from 'react'
import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const Chat = () => {
    return (
        <SafeAreaView>
            <Text>This is Chat Page under root tabs</Text>
        </SafeAreaView>
    );

}

export default Chat
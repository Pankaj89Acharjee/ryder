import React from 'react'
import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const Home = () => {
    return (
        <SafeAreaView>
            <Text>This is Home Page under root tabs</Text>
        </SafeAreaView>
    );

}

export default Home